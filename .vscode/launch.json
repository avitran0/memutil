{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "memutil",
            "cargo": {
                "args": ["build", "--bin=memutil", "--package=memutil"],
                "filter": {
                    "name": "memutil",
                    "kind": "bin"
                }
            },
            "args": ["watch", "131261", "48 83 3D ? ? ? ? 00 0F 95 C0 C3 @3/8 -> 1192 -> 208", "vec3"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
